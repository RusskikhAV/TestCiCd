pipeline {
    agent none
    stages {
        stage('Test run wih docker') {
            agent {
                docker {
                    image 'maven:3.8.1-adoptopenjdk-11'
                    args '-v C:/ProgramData/Jenkins/.jenkins/workspace/with_docker:/with_docker'
                    }
            }
            steps {
                echo 'Run tests'
                    bat 'mvn -version'
            }
        }
    }
}