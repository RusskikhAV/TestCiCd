pipeline {
    agent none
    stages {
        stage('Test run wih docker') {
            agent {
                docker { image 'maven:3.8.1-adoptopenjdk-11' }
            }
            steps {
                echo 'Run tests'
                    bat 'mvn -version'
            }
        }
    }
}